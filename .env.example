# Leave empty in local dev to use the Vite proxy (/api and /uploads -> backend).
# Set to your deployed backend origin for production builds (e.g. Netlify).
#
# Local backend example: http://localhost:5000
# Production example: https://be-blog-app-master-production.up.railway.app
VITE_API_URL=
